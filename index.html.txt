<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>QuickPick Express | Move it. Done.</title>

  <!-- Main stylesheet -->
  <link rel="stylesheet" href="style.css">

  <!-- Leaflet map -->
  <link rel="stylesheet"
        href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css">

  <!-- Optional theme color -->
  <meta name="theme-color" content="#0e4c69">
</head>
<body>

  <!-- Top Navigation -->
  <header class="topbar">
    <div class="brand">
      <h1>QuickPick Express</h1>
      <small>Lusaka delivery</small>
    </div>

    <button id="trackBtn" type="button">
      Track Delivery
    </button>
  </header>

  <!-- Main Content -->
  <main class="container">

    <!-- Hero -->
    <section class="hero">
      <h2>Move it. Done.</h2>
      <p>
        Fast motorbike delivery for Lusaka and beyond.
        Book a rider, see the price instantly, and track the trip live.
      </p>
    </section>

    <!-- Booking Form -->
    <section class="card">

      <h3>Book a Delivery</h3>

      <label for="serviceType">Service type</label>
      <select id="serviceType">
        <option value="package">Package delivery</option>
        <option value="passenger">Passenger ride</option>
        <option value="errand">Errand delivery</option>
      </select>

      <label for="pickup">Pickup location</label>
      <input id="pickup"
             type="text"
             placeholder="EastPark Mall, Lusaka">

      <label for="dropoff">Drop-off location</label>
      <input id="dropoff"
             type="text"
             placeholder="Kabulonga, Lusaka">

      <!-- Map controls -->
      <div class="map-controls">
        <button type="button" id="pickupMode">
          Set Pickup
        </button>

        <button type="button" id="dropoffMode">
          Set Drop-off
        </button>
      </div>

      <!-- Interactive map -->
      <div id="map"></div>

      <p class="map-hint">
        Click the map to place the selected location.
      </p>

      <label for="senderName">Sender name</label>
      <input id="senderName"
             type="text"
             placeholder="Your full name">

      <label for="senderPhone">Sender phone</label>
      <input id="senderPhone"
             type="tel"
             placeholder="097xxxxxxx">

      <label for="receiverName">Receiver name</label>
      <input id="receiverName"
             type="text"
             placeholder="Receiver full name">

      <label for="receiverPhone">Receiver phone</label>
      <input id="receiverPhone"
             type="tel"
             placeholder="096xxxxxxx">

      <label for="distance">Distance (km)</label>
      <input id="distance"
             type="number"
             value="5"
             min="1"
             readonly>

      <button id="quoteBtn" type="button">
        Get Price
      </button>

      <!-- Price output -->
      <div id="quoteBox" class="quote hidden">

        <p>
          Base fare:
          <span id="baseFare">K 0.00</span>
        </p>

        <p>
          Distance fee:
          <span id="distanceFee">K 0.00</span>
        </p>

        <p>
          Total:
          <strong id="totalFare">K 0.00</strong>
        </p>

      </div>

      <button id="bookBtn"
              type="button"
              class="hidden">
        Confirm Booking
      </button>

    </section>

    <!-- Saved bookings -->
    <section id="bookingsSection" class="card hidden">

      <h3>Saved Bookings</h3>

      <div id="bookingsList"></div>

    </section>

    <!-- Live tracking -->
    <section id="trackingSection" class="card hidden">

      <h3>Live Delivery Tracking</h3>

      <p>
        Booking:
        <strong id="trackingCode">-</strong>
      </p>

      <p id="trackingStatus">
        Waiting for booking...
      </p>

      <div id="trackingMap"></div>

      <!-- Delivery timeline -->
      <div class="timeline">

        <div class="timeline-item done">
          Request accepted
        </div>

        <div class="timeline-item done">
          Payment confirmed
        </div>

        <div class="timeline-item current"
             id="timelineTransit">
          Rider on the way
        </div>

        <div class="timeline-item"
             id="timelineDelivered">
          Delivered
        </div>

      </div>

    </section>
<section class="card auth-card">
  <h3>Account</h3>

  <input id="authEmail" type="email" placeholder="Email">
  <input id="authPassword" type="password" placeholder="Password">

  <button id="signupBtn" type="button">Sign Up</button>
  <button id="loginBtn" type="button">Sign In</button>
  <button id="logoutBtn" type="button">Sign Out</button>

  <p id="authStatus">Not signed in</p>
</section>
  </main>

  <!-- Footer -->
  <footer class="footer">
    <p>
      © 2026 QuickPick Express • Fast hands. Local heart.
    </p>
  </footer>

  <!-- Toast -->
  <div id="toast"></div>

  <!-- Leaflet -->
<script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
<script src="app.js"></script>
<script>
async function signUp() {
  const email = document.getElementById('authEmail').value;
  const password = document.getElementById('authPassword').value;

  const { error } = await supabaseClient.auth.signUp({
    email,
    password
  });

  if (error) return showToast(error.message);

  showToast('Check your email to confirm your account.');
}

async function signIn() {
  const email = document.getElementById('authEmail').value;
  const password = document.getElementById('authPassword').value;

  const { error } = await supabaseClient.auth.signInWithPassword({
    email,
    password
  });

  if (error) return showToast(error.message);

  showToast('Signed in.');
  updateAuthStatus();
}

async function signOut() {
  await supabaseClient.auth.signOut();
  updateAuthStatus();
  showToast('Signed out.');
}

async function updateAuthStatus() {
  const { data } = await supabaseClient.auth.getUser();

  document.getElementById('authStatus').textContent =
    data.user
      ? `Signed in as ${data.user.email}`
      : 'Not signed in';
}

document.getElementById('signupBtn').onclick = signUp;
document.getElementById('loginBtn').onclick = signIn;
document.getElementById('logoutBtn').onclick = signOut;

updateAuthStatus();
</script>

</body>
</html>